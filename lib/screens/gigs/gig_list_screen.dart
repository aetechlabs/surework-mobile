import 'package:flutter/material.dart';

class GigListScreen extends StatelessWidget {
  const GigListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Gigs')),
      body: const Center(
        child: Text('Gig List Screen - To be implemented'),
      ),
    );
  }
}
